// Re-export editor types from central location for backward compatibility
export type {
  RevisionSummary,
  RevisionFull,
  StashedContent,
  RevisionState,
  GenerationStatus,
  AIState,
} from '../types/editor'
